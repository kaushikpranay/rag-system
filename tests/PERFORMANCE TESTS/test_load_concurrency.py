# test_load_concurrency.py
# Load test — requests/sec before degradation (Locust)
# Concurrency test — correctness under simultaneous sessions
# Timeout / retry handling — graceful failure when Bedrock/Groq is slow
# Rate limit handling — behavior when upstream API throttles
