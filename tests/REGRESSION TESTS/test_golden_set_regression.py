# test_golden_set_regression.py
# Golden set re-run in CI on every prompt/model change
# Stops a "small prompt tweak" silently tanking faithfulness
# Version-tagged eval results — baseline.json vs hybrid_rerank.json
